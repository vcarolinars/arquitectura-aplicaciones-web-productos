apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: arquitectura-app
spec:
  selector:
    app: product-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
